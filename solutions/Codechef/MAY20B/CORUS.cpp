#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0); cin.tie(0);
    int t;
    cin >> t;
    while (t--)
    {
        int n, q;
        string s;
        cin >> n >> q >> s;
        vector<int> m(26, 0);
        for (auto c : s)
            m[c - 'a']++;
        while (q--)
        {
            int c;
            cin >> c;
        
        }
    }
}